doctype html
html(lang="en")
    head
        title Assignment 5: HTTP Cookie
    body
        if name
            h2 Welcome, #{name}!
        else 
            form(action="/trackName", method="post") 
                label Please enter your name: 
                    input(type="text", name="username")
                button(type="submit") Submit